name: smart_attendance_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # ✅ SMART ATTENDANCE PACKAGES
  camera: ^0.10.5
  http: ^1.6.0
  path_provider: ^2.1.5
  path: ^1.9.1
  flutter_image_compress: ^2.3.0  # <--- ADD THIS LINE HERE

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true