name: smart_attendance_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+8

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # Biometric option
  local_auth: ^2.3.0

  # Authentication
  supabase_flutter: ^2.8.0 

  # Storage (REQUIRED for security wrapper)
  shared_preferences: ^2.2.2

  # Core packages
  camera: ^0.10.5
  http: ^1.2.1
  path_provider: ^2.1.5
  path: ^1.9.1
  flutter_image_compress: ^2.3.0 
  flutter_dotenv: ^6.0.0
  image_picker: ^1.0.0
  webview_flutter: ^4.13.1
  wakelock_plus: ^1.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - .env
    - shorebird.yaml